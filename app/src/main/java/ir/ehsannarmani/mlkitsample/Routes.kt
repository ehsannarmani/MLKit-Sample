package ir.ehsannarmani.mlkitsample


enum class Routes(val route:String) {
    Home(route = "home"),
    FaceDetection(route = "face-detection"),
    FaceDrawer(route = "face-drawer"),
    FaceMesh(route = "face-mesh"),
    DocumentScanner(route = "document-scanner"),
}